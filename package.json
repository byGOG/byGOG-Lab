{
  "name": "bygog-lab",
  "private": true,
  "version": "0.1.0",
  "description": "byGOG kaynak arşivi",
  "scripts": {
    "build": "node scripts/build.js",
    "validate": "node scripts/validate.js",
    "check:encoding": "node scripts/check-encoding.js",
    "ci:validate": "npm run validate && npm run check:encoding",
    "fix:titles": "node scripts/fix-titles.js",
    "serve": "node scripts/serve.js",
    "optimize:svg": "node scripts/optimize-svg.mjs"
  },
  "devDependencies": {
    "csso": "^5.0.5",
    "esbuild": "^0.23.0",
    "playwright": "^1.56.0",
    "svgo": "^4.0.0"
  }
}
